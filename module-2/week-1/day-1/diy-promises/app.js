const btn = document.getElementById("do-action-in-close-future");

function getText() {}

function displayText() {}

function handleClick() {
    // this one will handle the click,
    // i.e : getting the text after a timeout
    // then, printing the text in the target element
}

btn.onclik = handleClick;